# Juridik-AI Project Dependencies
#
# Core dependencies for batch analysis and document processing
# Python 3.10+

# PDF Processing for RAG
# Primary: pdfplumber for advanced PDF handling with Swedish character support
pdfplumber>=0.10.0

# Fallback: PyPDF2 as alternative PDF library
PyPDF2>=4.0.0

# Token estimation for chunking
tiktoken>=0.5.0

# System monitoring for CLI
psutil>=5.9.0
rich>=13.0.0

# Optional: Ollama Python API client for direct model interaction
# If not installed, the scripts will fall back to curl/HTTP API
# Uncomment the line below to enable Python-based Ollama integration
# ollama>=0.1.0

# ChromaDB for vector storage
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Note: Most functionality relies on Python standard library (json, re, sys, pathlib, etc.)
# and local Ollama installation (http://localhost:11434)
